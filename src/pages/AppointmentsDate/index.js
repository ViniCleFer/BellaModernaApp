import React from 'react';
import { View } from 'react-native';

// import { Container } from './styles';

const AppointmentsDate = (props) => {
  console.tron.log(props);
  return <View />;
};

export default AppointmentsDate;
